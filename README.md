# CS609
Labs and HW from CS609 Class.

Assignment 1: Write your own HMM BaseCaller and FastQC
    -Question 1: Takes intensity scores from a csv and reads it into a HMM model for predicitive base calling, outputs as a fasta file.
    -Question 2: Reads a fastq file and gets the q score using bioPython.

Assignment 2: Write your own Suffix Array based algorithm for read search
 -Builds a suffix array and search through it to find the position of the reads, uses binary sort on the search pattern to find the left and right bounded indices since the suffix array is sorted.